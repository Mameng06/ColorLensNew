Build me an offline mobile app using React Native for the UI, and OpenCV with Java native modules for image processing. The app should use the device camera to show a live preview. There should be a fixed center point (like a crosshair or dot) on the screen. The user will align the object they want to detect in that center. The app should continuously detect the color at that center point in real time and display the color name or hex value as on-screen text. Also, use text-to-speech to read the detected color out loud in real time. Everything should work c
